name: "Corne Firmware"

on:
  workflow_dispatch:
  push:
    branches:
    - main
    paths:
    - config/corne.*

jobs:
  corne:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
